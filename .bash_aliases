#
# |----------------------------------------------------------|
# |                                                          |
# |                       BASH ALIASES                       |
# |                                                          |
# |----------------------------------------------------------|
#
# This file manages bash aliases.
#
# Whilst most aliases should be here, aliases tied to a
# specific configuration might be elsewhere.
# For instance, the emacs config uses an alias for "emacs",
# but since the additional emacs config is set in .bashrc,
# the alias is set over there.
#

alias cd="1>/dev/null cd"  # make cd not print anything
alias sl=ls
